import React, { Component } from "react";
import "../css/Albums.css";

export default class Albums extends Component {
    // Renders the component
    render() {
        return <div className="albums_wrapper" />;
    }
}
